import Model from "../users/models.js";
import View from "../users/views.js";
import Controller from "../users/controllers.js";

// eslint-disable-next-line no-unused-vars
const controller = new Controller(new Model(), new View());
